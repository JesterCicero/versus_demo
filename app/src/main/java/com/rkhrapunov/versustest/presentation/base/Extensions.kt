package com.rkhrapunov.versustest.presentation.base

import timber.log.Timber

//fun <T> List<T>.listsEqual(listToCompare: List<T>) = size == listToCompare.size && zip(listToCompare).all { (element1, element2) -> element1 == element2 }

//fun <T> List<T>.listsEqual(listToCompare: List<T>): Boolean {
//    if (size != listToCompare.size) return false
//    val pairList = zip(listToCompare)
//    val result = pairList.all { (element1, element2) -> element1 == element2 }
//    Timber.d("####### result is: $result")
//    return result
//}

